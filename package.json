{
  "name": "static-site-deploy",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "serve": "node server",
    "test": "npm run build && concurrently \"npm run serve\" \"node test\""
  },
  "dependencies": {
    "astro": "^5.5.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "express": "^4.19.2",
    "puppeteer": "^23.1.0"
  }
}
